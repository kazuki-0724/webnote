{
  "name": "webnote",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "hosting:deploy": "npm run build && npx firebase-tools deploy --only hosting",
    "hosting:deploy:project": "npm run build && npx firebase-tools deploy --only hosting --project"
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "pinia": "^2.3.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^6.0.0"
  }
}
